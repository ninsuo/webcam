{% macro webcam_js() %}

    <script type="text/javascript">

        setInterval(function () {

            var val = {{ constant('App\\Constants::SLIDER') }};

            if ($('#slider').length) {
                val = $('#slider').slider("option", "value");
            }

            if (val === {{ constant('App\\Constants::SLIDER') }}) {
                $('.live').each(function () {
                    var that = $(this);
                    that.attr('src', (that.data('src') + '?tm=' + (Date.now() / 1000 | 0)));
                });
            }

        }, 1000);

    </script>

{% endmacro %}
